apiVersion: v1
kind: Service
metadata:
  name: patient-service # This name becomes the internal DNS URL (http://patient-service:3002)
  namespace: healthbridge
spec:
  selector:
    app: patient-service
  ports:
    - protocol: TCP
      port: 3002
      targetPort: 3002