apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: healthbridge
spec:
  selector:
    app: ai-service
  ports:
    - protocol: TCP
      port: 3007
      targetPort: 3007
  type: ClusterIP